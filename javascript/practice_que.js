// Que-1 Print all even numbers from 0 to 100
for(let i =0; i<=10; i++){
    if(i % 2 === 0){
        console.log(i);
    }
}

// Que-2 game starts with any random numbers. Ask the  user to keep guessing the game number until the user enters correct value
let gameNum = 25;
let input =  prompt("guess the num");
console.log(input);
